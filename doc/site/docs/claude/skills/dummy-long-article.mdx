---
title: "dummy-long-article"
description: "A dummy skill with a longer article body for testing scroll behavior, table of contents, and content rendering in the doc site. Use when: (1) Testing long content rendering, (2) Debugging TOC generati..."
---

# dummy-long-article



# Dummy Long Article

This skill has a longer body to test how the doc site handles extended content, table of contents sidebar, scroll behavior, and anchor links.

## Section 1: Introduction

Lorem ipsum dolor sit amet. This section tests basic paragraph rendering and heading anchor generation.

Some key points:

- Point one about testing
- Point two about debugging
- Point three about verification

## Section 2: Code Examples

### JavaScript

```javascript
function greet(name) {
  console.log(`Hello, ${name}!`);
}

greet("World");
```

### Python

```python
def greet(name: str) -> None:
    print(f"Hello, {name}!")

greet("World")
```

### Bash

```bash
#!/bin/bash
echo "Hello, World!"
```

## Section 3: Tables

### Simple Table

| Column A | Column B | Column C |
|----------|----------|----------|
| Value 1 | Value 2 | Value 3 |
| Value 4 | Value 5 | Value 6 |

### Wide Table

| Name | Type | Default | Required | Description |
|------|------|---------|----------|-------------|
| `alpha` | `string` | `""` | Yes | First parameter |
| `beta` | `number` | `0` | No | Second parameter |
| `gamma` | `boolean` | `false` | No | Third parameter |
| `delta` | `object` | `{}` | No | Fourth parameter |

## Section 4: Nested Lists

1. First level item
   - Second level item A
   - Second level item B
     - Third level item
   - Second level item C
2. Another first level item
   1. Ordered sub-item
   2. Another ordered sub-item

## Section 5: Blockquotes and Admonitions

> This is a blockquote. It tests how quoted text is rendered in the documentation.

> **Note:** This is an important note within a blockquote.

## Section 6: Inline Formatting

This paragraph contains **bold text**, *italic text*, `inline code`, and ~~strikethrough text~~. It also has [a link](#section-1-introduction) back to section 1.

## Section 7: Edge Cases

### Special Characters

Testing angle brackets in non-code context: use `&lt;placeholder&gt;` syntax for variables.

### Long Lines

This is a very long line that should test how the documentation site handles horizontal overflow and text wrapping in the main content area of the page without breaking the layout or causing horizontal scrollbars to appear unexpectedly.

### Empty Section

(intentionally left mostly empty for testing)

## Summary

This dummy skill tested:

- Heading anchors and TOC generation
- Code block rendering (JS, Python, Bash)
- Table rendering (simple and wide)
- Nested lists (ordered and unordered)
- Blockquotes
- Inline formatting
- Special characters and edge cases

